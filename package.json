{
  "name": "simmatrix",
  "version": "0.4.8",
  "private": true,
  "//type": "src/shared/bus.js är vår enda .js och den är en ES-modul. Utan detta läser Node om den som CommonJS först och varnar vid varje testkörning — brus i CI-loggen som döljer det man faktiskt vill se.",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "node tests/overlay-delta-bar.mjs && node tests/overlay-inputs-trace.mjs && node tests/overlay-loop.mjs && node tests/overlay-gate.mjs && node tests/overlay-options.mjs && node tests/overlay-preview.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
