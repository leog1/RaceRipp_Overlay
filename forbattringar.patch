diff --git a/.gitignore b/.gitignore
index 64ea682..ba0c4b3 100644
--- a/.gitignore
+++ b/.gitignore
@@ -17,6 +17,8 @@ engine/dist/
 engine/*.spec
 .venv/
 venv/
+# MoTeC .ld-parser (GPL-3.0) — hålls lokalt, committas ej i detta MIT-repo
+engine/ldparser.py
 
 # ── OS / editor ──
 .DS_Store
diff --git a/engine/requirements.txt b/engine/requirements.txt
index 1fb1bea..d2afce5 100644
--- a/engine/requirements.txt
+++ b/engine/requirements.txt
@@ -2,5 +2,6 @@ websockets>=12.0
 numpy>=1.24
 # ACC delat minne (Windows). Saknas den faller motorn tillbaka på mock.
 pyaccsharedmemory ; sys_platform == "win32"
-# MoTeC .ld-parser (EN fil, ej pip-paket) — hämta ldparser.py från:
-#   https://github.com/gotzl/ldparser  → lägg i engine/
+# MoTeC .ld-parser (GPL-3.0, EN fil, ej pip-paket) — committas EJ i detta MIT-repo.
+# Hämta ldparser.py från https://github.com/gotzl/ldparser och lägg i engine/.
+# Filen är gitignorerad och behövs bara för MoTeC-referens, inte för ACC-data.
diff --git a/src-tauri/src/lib.rs b/src-tauri/src/lib.rs
index 5f42025..4046ea5 100644
--- a/src-tauri/src/lib.rs
+++ b/src-tauri/src/lib.rs
@@ -98,6 +98,9 @@ fn default_state_for(d: &OverlayDef) -> OverlayState {
 struct Settings {
     overlays: HashMap<String, OverlayState>,
     reference_ld: String,
+    // global: visa overlays först när motorn är synkad mot ACC (connected==true)
+    #[serde(default)]
+    hide_until_connected: bool,
 }
 
 fn default_settings() -> Settings {
@@ -364,6 +367,30 @@ fn set_reference(app: AppHandle, state: State<Mutex<Settings>>, path: String) {
     }
 }
 
+// ── Globala inställningar (gäller alla overlays) ─────────────────────────────
+#[derive(Serialize, Clone)]
+struct GlobalsPayload {
+    hide_until_connected: bool,
+}
+
+#[tauri::command]
+fn get_globals(state: State<Mutex<Settings>>) -> GlobalsPayload {
+    let s = state.lock().unwrap();
+    GlobalsPayload { hide_until_connected: s.hide_until_connected }
+}
+
+// Visa overlays först när motorn är ansluten till ACC. Skickas till alla fönster;
+// overlays döljer sig själva (via bus.js) tills connected==true.
+#[tauri::command]
+fn set_hide_until_connected(app: AppHandle, state: State<Mutex<Settings>>, value: bool) {
+    {
+        let mut s = state.lock().unwrap();
+        s.hide_until_connected = value;
+        save_settings(&app, &s);
+    }
+    let _ = app.emit("globals", GlobalsPayload { hide_until_connected: value });
+}
+
 // ── App-uppstart ────────────────────────────────────────────────────────────
 #[cfg_attr(mobile, tauri::mobile_entry_point)]
 pub fn run() {
@@ -400,7 +427,9 @@ pub fn run() {
             set_option,
             reset_overlay,
             set_edit_mode,
-            set_reference
+            set_reference,
+            get_globals,
+            set_hide_until_connected
         ])
         .setup(move |app| {
             let handle = app.handle().clone();
diff --git a/src/control-panel/index.html b/src/control-panel/index.html
index 3d22cfd..3b8d3cc 100644
--- a/src/control-panel/index.html
+++ b/src/control-panel/index.html
@@ -44,6 +44,11 @@ body{font-family:var(--font);background:var(--app-bg);color:var(--ink);
 .list-head{padding:16px 18px 10px;font-size:11px;font-weight:700;letter-spacing:2px;
   text-transform:uppercase;color:var(--amber);}
 #list{flex:1 1 auto;overflow-y:auto;padding:0 8px 12px;}
+.list-foot{flex:0 0 auto;display:flex;align-items:center;gap:10px;
+  padding:12px 16px;border-top:1px solid var(--divider);background:var(--rail-bg);}
+.list-foot .lf-text{flex:1 1 auto;min-width:0;display:flex;flex-direction:column;gap:2px;}
+.list-foot .lf-title{font-size:12px;font-weight:600;}
+.list-foot .lf-sub{font-size:10px;color:var(--faint);}
 .lrow{width:100%;appearance:none;background:none;border:0;color:var(--ink);font:inherit;cursor:pointer;
   display:flex;align-items:center;gap:10px;padding:11px 12px 11px 9px;border-radius:9px;
   border-left:3px solid transparent;text-align:left;transition:background .12s ease;}
@@ -167,6 +172,13 @@ button.act:hover{background:var(--ctrl-hi);}
     <section class="list">
       <div class="list-head">Overlays</div>
       <div id="list"><div class="empty">Laddar …</div></div>
+      <div class="list-foot">
+        <div class="lf-text">
+          <span class="lf-title">Endast när ACC kör</span>
+          <span class="lf-sub">Dölj tills bilen rullar</span>
+        </div>
+        <label class="sw" id="gateSwitch"><input type="checkbox"><span class="track"></span><span class="knob"></span></label>
+      </div>
     </section>
 
     <!-- ── höger detaljpanel ── -->
@@ -451,7 +463,19 @@ if (T && T.event) T.event.listen('edit-mode', e => { editMode = !!e.payload; $('
 /* ── version ── */
 try { T?.app?.getVersion?.().then(v => { if (v) $('omVer').textContent = 'v' + v; }).catch(() => {}); } catch {}
 
-document.fonts.ready.then(loadOverlays);
+/* ── global: visa overlays endast när ACC kör ── */
+$('gateSwitch').querySelector('input').addEventListener('change', async e => {
+  try { await invoke('set_hide_until_connected', { value: e.target.checked }); }
+  catch(err){ toast('Kunde inte spara inställningen: ' + errMsg(err), true); }
+});
+async function loadGlobals(){
+  try {
+    const g = await invoke('get_globals');
+    $('gateSwitch').querySelector('input').checked = !!(g && g.hide_until_connected);
+  } catch {}
+}
+
+document.fonts.ready.then(() => { loadOverlays(); loadGlobals(); });
 </script>
 </body>
 </html>
diff --git a/src/shared/bus.js b/src/shared/bus.js
index 581ea4d..d0b42ab 100644
--- a/src/shared/bus.js
+++ b/src/shared/bus.js
@@ -32,7 +32,12 @@ export class WsBus {
     if (this._last) fn(this._last);
     return () => this._subs.delete(fn);
   }
-  _emit(f) { this._last = f; for (const fn of this._subs) fn(f); }
+  _emit(f) {
+    this._last = f;
+    _lastConnected = !!(f && f.connected);
+    _applyGate();
+    for (const fn of this._subs) fn(f);
+  }
   _connect() {
     let ws;
     try { ws = new WebSocket(this.url); }
@@ -44,6 +49,16 @@ export class WsBus {
   }
 }
 
+// ── Synk-grind: dölj overlayn tills motorn är ansluten (global inställning) ──
+// Styrs av kontrollpanelens "Endast när ACC kör". Utan Tauri (OBS/webbläsare)
+// är grinden av → overlayn syns alltid.
+let _hideUntilConnected = false;
+let _lastConnected = false;
+function _applyGate() {
+  const hidden = _hideUntilConnected && !_lastConnected;
+  document.documentElement.style.visibility = hidden ? 'hidden' : '';
+}
+
 // ── Skal-integration: lyssna på kontrollpanelens config + edit-läge ──────────
 // applyConfig får {scale, opacity}. applyOption (valfri) får (id, value) per
 // alternativ som overlayn deklarerat i registry.json. Fungerar även utan Tauri
@@ -56,6 +71,19 @@ export function wireShell(applyConfig, applyOption) {
   let label = null;
   try { label = T.window.getCurrentWindow().label; } catch {}
 
+  // Global grind: visa overlays först när ACC är ansluten ("Endast när ACC kör").
+  try {
+    T.core.invoke('get_globals').then(g => {
+      _hideUntilConnected = !!(g && g.hide_until_connected);
+      _applyGate();
+    }).catch(() => {});
+  } catch {}
+  T.event.listen('globals', (e) => {
+    const p = e.payload || {};
+    _hideUntilConnected = !!p.hide_until_connected;
+    _applyGate();
+  });
+
   // Hämta sparad skala/opacitet/alternativ direkt vid start (undviker fel look först).
   try {
     T.core.invoke('get_config', { id: label }).then(cfg => {
